/**
 * VoidRenderer is a simple implementation of the Renderer interface that does not perform any rendering.
 */
public class VoidRenderer implements Renderer {
    public void renderBoard(Board board){}
}
